import 'package:flutter/material.dart';

BoxShadow commonShadow() => BoxShadow(
      offset: const Offset(0, 10),
      blurRadius: 10,
      spreadRadius: 1,
      color: Colors.black.withOpacity(.4),
    );
